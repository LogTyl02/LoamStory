KYLE SCHAEFER

Grid class has several methods just added different types of "objects" such as walls markObject method

The monst class has a wonder mode, that is aware of objects and cannot go through has to wonder within the boundries
